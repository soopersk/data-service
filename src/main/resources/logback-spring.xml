<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- LOCAL profile: human-readable plain text -->
    <springProfile name="local">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [reqId=%X{requestId:-} tenant=%X{tenant:-} calc=%X{calculatorId:-} run=%X{runId:-}] - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- NON-LOCAL profiles (dev, prod): structured JSON -->
    <springProfile name="!local">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- request/job context -->
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>tenant</includeMdcKeyName>
                <includeMdcKeyName>calculatorId</includeMdcKeyName>
                <includeMdcKeyName>runId</includeMdcKeyName>
                <!-- alert delivery keys — required for App Insights custom dimensions / BigPanda routing -->
                <includeMdcKeyName>alert_source</includeMdcKeyName>
                <includeMdcKeyName>alert_type</includeMdcKeyName>
                <includeMdcKeyName>calculatorName</includeMdcKeyName>
                <includeMdcKeyName>tenantId</includeMdcKeyName>
                <includeMdcKeyName>severity</includeMdcKeyName>
                <includeMdcKeyName>breachType</includeMdcKeyName>
                <includeMdcKeyName>expectedValue</includeMdcKeyName>
                <includeMdcKeyName>expectedUnit</includeMdcKeyName>
                <includeMdcKeyName>actualValue</includeMdcKeyName>
                <includeMdcKeyName>actualUnit</includeMdcKeyName>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

</configuration>
